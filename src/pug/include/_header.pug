h1(class="headerSiteName ttl--1")
  a(href="index.html") #{siteName}
  
ul(class="headerMenu")
  li
    if key == "websites"
      a(href=pages.websites.url class="headerMenu__here") websites/apps
    else
      a(href=pages.websites.url) websites/apps
  li
    if key == "drawings"
      a(href=pages.drawings.url class="headerMenu__here") drawings
    else
      a(href=pages.drawings.url) drawings
  li
    if key == "photos"
      a(href=pages.photos.url class="headerMenu__here") photos
    else
      a(href=pages.photos.url) photos
  li
    if key == "riddles"
      a(href=pages.riddles.url class="headerMenu__here") riddles
    else
      a(href=pages.riddles.url) riddles
